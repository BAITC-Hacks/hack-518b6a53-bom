services:
  ui:
    volumes:
      - ./frontend:/app:ro
    command: ["node", "--watch", "server.js"]

  api:
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./backend:/app/backend:ro
      - ./shared:/app/shared:ro
      - ./data/tech2-v1:/app/data/tech2-v1:ro
    command: ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
